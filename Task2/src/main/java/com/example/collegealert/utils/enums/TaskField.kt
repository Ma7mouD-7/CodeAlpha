package com.example.collegealert.utils.enums

import com.example.collegealert.R

enum class TaskField(val colorId: Int) {
    LECTURE(R.color.orange),
    MILESTONE(R.color.blue),
    EXAM(R.color.pink),
}
